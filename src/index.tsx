import React from "react";
import { createRoot } from "react-dom/client";
import BlackFlameCursor from "./BlackFlameCursor";

const root = createRoot(document.getElementById("root") as Element);

root.render(<BlackFlameCursor />);
